//area of square

#include<stdio.h>
#include<stdlib.h>

int main() {
    int side;
    printf("enter side: ");
    scanf("%d", &side);

    printf("area of the square is %d :)", side * side);
    return 0;
}