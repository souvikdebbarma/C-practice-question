// take a number (n) from user and output it's cube (n*n*n)

#include <stdio.h>
#include <stdio.h>

int main() {
    int n;

    printf("enter the number: ");
    scanf("%d", &n);

    printf("the volume of cube is %d", n * n * n);
    return 0;

}