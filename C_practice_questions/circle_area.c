// area of a circle

#include<stdio.h>
#include<stdlib.h>

int main() {
    float radius;

    printf("enter radius: ");
    scanf("%f", &radius);

    printf("the radius of the circle is: %f", 3.14 * radius * radius);
    return 0;
}